package org.example.facileIntermediaire.calculatrice;

import java.io.File;
import java.io.FileReader;

/*
Vous devez créer une classe Calculatrice avec des méthodes pour effectuer des opérations mathématiques simples
telles que l'addition, la soustraction, la multiplication et la division. Cependant, vous devez également gérer
les cas d'erreur possibles tels que la division par zéro.

Définissez une classe Calculatrice avec les méthodes suivantes :

addition(double a, double b) : Cette méthode prend deux nombres en entrée et renvoie leur somme.
soustraction(double a, double b) : Cette méthode prend deux nombres en entrée et renvoie leur différence (a - b).
multiplication(double a, double b) : Cette méthode prend deux nombres en entrée et renvoie leur produit.
division(double a, double b) : Cette méthode prend deux nombres en entrée et renvoie leur quotient (a / b).
Gérez les cas d'erreur possibles, notamment la division par zéro. Si la division par zéro est détectée,
lancez une exception appropriée.

Utilisez try/catch pour capturer les exceptions et afficher un message d'erreur explicite en cas d'erreur.

Ajoutez une méthode calculer qui prend en entrée deux nombres, une opération (par exemple, "+", "-", "*", "/"),
et effectue l'opération appropriée en utilisant les méthodes précédemment définies.

Ajoutez une méthode main pour tester votre implémentation en invoquant différentes opérations mathématiques
avec différents types d'entrées.
 */
public class Exercice {


	public static void main(String[] args) {
//        Exercice calc = new Exercice();
//
//        // Exemples d'utilisation
//        double a = 10;
//        double b = 0;
//        String operation = "/";
//        System.out.println("Résultat : " + calc.calculer(a, b, operation));
	}
}
